
repos:
  - id: /github.com/dracula33/devops-netology/
    allow_custom_workflows: true
    workflow: custom

workflows:
  custom:
    plan:
      steps:
        - run: echo INIT
        - init
        - run: echo PLAN
        - plan:
            extra_args: ["-lock", "false"]
    apply:
      steps:
        - run: echo APPLY
        - apply

